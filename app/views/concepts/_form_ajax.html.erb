<%= form_for @concept, remote: true do |f| %>
  <% if @concept.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concept.errors.count, "error") %> prohibited this concept from being saved:</h2>

      <ul>
      <% @concept.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :name, class:"sr-only" %>
    <%= f.text_field :name, placeholder: "What is this question mainly about? Could you list the main concept(s)?", class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit class: 'btn-submit' %>
  </div>
<% end %>
