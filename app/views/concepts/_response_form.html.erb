<%= form_tag responses_path(@concept.id), id: "concept#{@concept.id}_response", method: :post, remote: true, authenticity_token: true do %>
  <div class="form-group">
    <label for="concept[body]" class="sr-only">Response</label>
    <input type="text" name="concept[body]" placeholder="Short is good" class="form-control">
  </div>

  <input type="submit" name="name" value="Respond" class="btn btn-submit">
<% end %>
