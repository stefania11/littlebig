
  var $responseForm = $('#concept<%= @concept.id %>_response');
  var $conceptFooter = $('#mention_<%= @concept.id %> .post-bottom');
  var $newConceptFooter = $('<%= j render partial: "concepts/new_mention_btn", locals: { concept: @concept } %>').hide();

  $responseForm.slideUp(function(){
    $(this)
      .remove();

    $conceptFooter.slideUp(function(){
      $(this)
        .replaceWith($newConceptFooter);

      $('#concept-<%= @concept.id %> .post-bottom').slideDown();
    })
  });

$('#blog').prepend('<%= j render 'layouts/flash' %>').hide().fadeIn();

setTimeout(function(){
  $('.alert').fadeOut(function(){
    $(this).remove();
  })
}, 3000);
